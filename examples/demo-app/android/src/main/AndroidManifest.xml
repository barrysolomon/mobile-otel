<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".DemoApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Schedulr demo app (launcher) -->
        <activity
            android:name=".SchedulingActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ShopSocialActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ConfigActivity"
            android:label="OTel Configuration"
            android:exported="false" />

        <activity
            android:name=".Dash0ConfigActivity"
            android:label="Dash0 Connection"
            android:exported="false" />

        <activity
            android:name=".AboutActivity"
            android:label="About"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".HelpActivity"
            android:label="Help"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".LogsActivity"
            android:label="Logs"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".auth.LoginActivity"
            android:label="Login"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".ui.debug.RingBufferActivity"
            android:label="Ring Buffer"
            android:parentActivityName=".SchedulingActivity"
            android:exported="false" />

        <!-- FileProvider for sharing exported logs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
